module.exports = {
  i18n: {
    // These are all the locales we support in our app
    locales: ['en', 'ru'],
    // This is the default locale to be used when visiting a non-locale prefixed path e.g. `/hello`
    defaultLocale: 'en'
  }
};
